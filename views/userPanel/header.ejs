<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Header</title>
</head>
<style>
    :root {
        --primary-color: rgba(255, 68, 0, 0.679);
        --hover-color: rgba(255, 68, 0, 0.805);
        --background-color: #f9f9f9;
        --text-color: #333;
        --light-color: #fff;
        --box-shadow: 0 4px 10px rgba(0, 0, 0, 0.1);
        --border-radius: 8px;
    }

    /* Global Reset */
    body {
        margin: 0;
        font-family: Arial, sans-serif;
        background-color: var(--background-color);
        color: var(--text-color);
    }

    a {
        text-decoration: none;
        color: var(--primary-color);
        transition: color 0.3s ease;
    }

    a:hover {
        color: var(--hover-color);
    }

    h1 {
        font-size: 2em;
        margin: 20px 0;
        text-align: center;
    }

    /* Header Styles */
    header {
        display: flex;
        align-items: center;
        justify-content: space-between;
        padding: 10px 20px;
        background-color: var(--primary-color);
        color: var(--light-color);
        box-shadow: var(--box-shadow);
    }

    /* Logo */
    .logo img {
        max-width: 100%;
        height: auto;
        border-radius: var(--border-radius);
    }

    /* Navigation */
    nav {
        display: flex;
        align-items: center;
        gap: 20px;
    }

    nav ul {
        display: flex;
        list-style: none;
        margin: 0;
        padding: 0;
        gap: 20px;
    }

    nav ul li a {
        color: var(--light-color);
        font-weight: 600;
        padding: 5px 10px;
        border-radius: var(--border-radius);
        transition: background-color 0.3s ease;
    }

    nav ul li a:hover {
        background-color: var(--hover-color);
        color: white;
    }

    /* Sorting Form and Search Bar */
    .sorting-form,
    .search-bar {
        display: flex;
        gap: 10px;
        align-items: center;
    }

    .sorting-form select,
    .search-bar input[type="text"] {
        padding: 8px 12px;
        font-size: 1em;
        border: 1px solid #ddd;
        border-radius: var(--border-radius);
        transition: border-color 0.3s ease;
    }

    .sorting-form select:focus,
    .search-bar input[type="text"]:focus {
        outline: none;
        border-color: var(--primary-color);
        box-shadow: 0 0 6px var(--primary-color);
    }

    .sorting-form button,
    .search-bar input[type="submit"],
    .search-bar button {
        padding: 8px 16px;
        font-size: 1em;
        background-color: var(--primary-color);
        color: var(--light-color);
        border: none;
        border-radius: var(--border-radius);
        cursor: pointer;
        transition: background-color 0.3s ease;
    }

    .sorting-form button:hover,
    .search-bar input[type="submit"]:hover,
    .search-bar button:hover {
        background-color: var(--hover-color);
    }
</style>
<body>
    <header>
        <div class="logo"><img src="/users/img/img-logo.png" width="80px" height="50px" alt="Logo"></div>
        <nav>
            <ul>
                <li><a href="/">Home</a></li>
                <% if(locals.user){ %>
                    <%= locals.user.username %>
                <% } else { %>
                <li><a href="/userLogin">Login</a></li>
                <% } %>

                <% if(locals.user){ %>
                    <li><a href="/userlogout">Logout</a></li>
                <% } else { %>
                    <li><a href="/userRegister">Register</a></li>
                <% } %>
            </ul>
    
            <form class="sorting-form" action="/sortings" method="get">
                <select name="sortings" id="sortings">
                    <option value="">--sorting--</option>
                    <option value="Asc">Ascending</option>
                    <option value="Desc">Descending</option>
                </select>
                <button type="submit">Sort</button>
            </form>
    
            <div class="search-bar">
                <form action="" style="display: flex; align-items: center; gap: 10px; margin: 20px 0;">
                    <input type="text" name="search" placeholder="Search...">
                    <input type="submit" value="Search">
                    <a href="/"><button type="button">Back</button></a>
                </form>
            </div>
        </nav>
    </header>
</body>
</html>